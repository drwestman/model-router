# @drwestman/model-router-core

Host-agnostic config, policy, prompt, model-normalization, and state-path helpers for model-router adapters.

This private workspace package intentionally avoids host runtime bindings.
