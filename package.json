{
  "name": "opencode-model-router",
  "version": "1.1.16",
  "description": "OpenCode plugin that routes tasks to tiered subagents (fast/medium/heavy) based on complexity",
  "type": "module",
  "main": "./src/index.js",
  "license": "GPL-3.0-only",
  "engines": {
    "node": "^20.0.0 || ^22.0.0 || >=24.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/marco-jardim/opencode-model-router.git"
  },
  "homepage": "https://github.com/marco-jardim/opencode-model-router",
  "bugs": {
    "url": "https://github.com/marco-jardim/opencode-model-router/issues"
  },
  "author": {
    "name": "Marco Jardim",
    "url": "https://github.com/marco-jardim"
  },
  "keywords": [
    "opencode",
    "claude-code",
    "plugin",
    "model-routing",
    "subagents",
    "delegation",
    "multi-model",
    "orchestration"
  ],
  "files": [
    "src/index.js",
    "src/templates/delegation-protocol.md",
    "src/opencode-ai-plugin.d.ts",
    "tiers.json",
    "LICENSE",
    "README.md"
  ],
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.0.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "install:local": "npm run prepack && npm --prefix ~/.config/opencode install .",
    "pretest": "npm run build && npm run typecheck",
    "prepack": "npm run build && npm run typecheck",
    "test": "npm run test:unit && npm run test:bdd && npm run test:package",
    "test:unit": "node --import tsx --test test/unit/**/*.test.ts",
    "test:bdd": "node --import tsx ./node_modules/@cucumber/cucumber/bin/cucumber-js test/bdd/features/**/*.feature --import test/bdd/steps/**/*.ts",
    "test:package": "node scripts/verify-pack.mjs"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.2.0",
    "@types/node": "^25.2.3",
    "tsx": "^4.20.5",
    "typescript": "^5.9.3"
  }
}
